import { StyledHeader } from "./styles/Header.styled";

function Header() {
    return (
        <StyledHeader>
            <header>
                <div>
                    <h1>DOCAPP</h1>
                </div>
            </header>
        </StyledHeader>
    );
}

export default Header;